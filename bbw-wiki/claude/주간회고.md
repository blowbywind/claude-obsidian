
## 2026-06-28

## ① 주요 성과/수정

- **런타임 3-way fallback 완성**: Codex·agy·Claude 교차 fallback 배선, 토큰 소진 시 자동 전환 — Chat 경로·Projects 경로 모두 적용
- **봇 자율 sudo 시스템 구축**: 컨테이너 봇 → 백엔드 큐 → 호스트 executor 흐름, 승인 UI + SSE 안정화
- **DB 스키마·실행 경로 SSOT 중앙화**: `schema.sql` 단일 출처, `runtime_providers.binary` 단일 출처 리팩터
- **Cron 자율 기록 안전 재구현**: LLM 읽기전용 + executor 결정론 append — `dex-session-log`(일 1회)·`dex-weekly-review`(주 1회) 복구
- **Codex 토큰 사용량 계측 추가**: 관측 사각 해소, `usage-api` 경로 신설
- **시드 픽스**: `seed-v2/v3` 재시드 시 라이브 봇 게이트웨이·모델 설정이 초기화되던 문제 차단

---

## ② 반복된 문제·불안정

- **인가 로직 반복 누락**: sudo 승인/거부 인가를 네트워크 출처 게이트로 뒤늦게 전환 — 초기 설계 시 인가 경계가 명확하지 않아 fix가 누적됨
- **레이스 컨디션**: 거부 후 봇 재제출 → 중복 승인카드 생성 버그가 sudo 기능 구현과 동시에 발생
- **SPA · API 라우트 동명 충돌**: 직접 URL 로드 시 404 — 페이지 라우트와 API 라우트 네이밍 충돌이 신규 기능 추가마다 재발 패턴
- **런타임 메타 불일치**: agy 심링크 깨짐 내성 추가, Gemini 스테일 메타데이터 픽스 — 바이너리 경로·메타를 수동 관리하는 구조상 반복 위험
- **Cron 로직 불안정**: 자율 기록 cron이 안전하지 않아 전면 재구현 — 초기 구현이 LLM 쓰기 접근을 허용하는 설계 결함

---

## ③ 다음 주 권장

- **인가 계층 통합**: 네트워크 출처 게이트·세션 게이트·sudo 인가를 단일 미들웨어로 일원화 — 분산된 인가 로직이 반복 버그의 근원
- **런타임 메타 자동 검증**: 바이너리 경로·심링크 유효성을 서버 시작 시 자동 점검하는 헬스체크 추가
- **SPA 라우트 충돌 방지 규칙**: API 경로를 `/api/` 프리픽스로 강제하거나 페이지 라우트 예약 목록 관리 — 동명 충돌 구조적 차단
- **fallback 체인 통합 테스트**: 3-way fallback 엣지케이스(전체 소진, 부분 소진, 순서 역전) 커버하는 테스트 작성 (`runtime-auth.test.ts` 확장)
- **Cron executor 감사 로그**: append 결과(성공/실패/중복) 영구 기록 — 재구현 후 실제 동작 검증 근거 확보
